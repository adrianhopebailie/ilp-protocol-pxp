{
  "name": "ilp-transport",
  "version": "0.0.0",
  "description": "Interledger packet transport",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "build/**/*.js",
    "build/**/*.js.map",
    "build/**/*.d.ts",
    "build/doc"
  ],
  "scripts": {
    "clean": "rm -Rf .nyc_output && rm -Rf coverage && rm -Rf build ",
    "build": "tsc",
    "lint": "tslint --project .",
    "pretest": "tsc --build",
    "test": "nyc mocha --colors --require ts-node/register --ui mocha-typescript --require source-map-support/register test/**/*.test.ts",
    "doc": "typedoc --options typedoc.js src/index.ts",
    "codecov": "codecov",
    "preversion": "npm run lint && npm test",
    "prepublishOnly": "npm run lint && npm test && npm run doc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/interledgerjs/ilp-transport.git"
  },
  "author": "Interledger Team <info@interledger.org>",
  "license": "Apache-2.0",
  "dependencies": {
    "@types/verror": "^1.10.3",
    "ilp-logger": "^1.2.1",
    "ilp-packet": "^3.0.9",
    "oer-utils": "^4.0.0",
    "verror": "^1.10.0"
  },
  "devDependencies": {
    "@types/chai": "^4.2.4",
    "@types/chai-as-promised": "^7.1.2",
    "@types/mocha": "^5.2.7",
    "@types/node": "^10.17.0",
    "@types/sinon": "^5.0.7",
    "@types/ws": "^6.0.3",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "codecov": "^3.6.1",
    "ilp-protocol-ildcp": "^2.0.2",
    "mocha": "^5.2.0",
    "mocha-typescript": "^1.1.17",
    "nyc": "^13.3.0",
    "sinon": "^7.5.0",
    "source-map-support": "^0.5.13",
    "ts-node": "^7.0.1",
    "tslint": "^5.20.0",
    "tslint-config-standard": "^8.0.1",
    "typedoc": "^0.13.0",
    "typescript": "^3.6.4",
    "ws": "^6.2.1"
  },
  "nyc": {
    "check-coverage": true,
    "lines": 80,
    "statements": 80,
    "functions": 80,
    "branches": 80,
    "include": [
      "src/**/*"
    ],
    "exclude": [
      "test"
    ],
    "reporter": [
      "lcov",
      "text-summary"
    ],
    "extension": [
      ".ts",
      ".tsx"
    ],
    "sourceMap": true,
    "instrument": true,
    "all": true
  }
}
